#!/bin/bash

# Script for setting up fail2ban to protect against brute-force attacks
# Install fail2ban: apt-get update && apt-get install -y fail2ban
# Configure fail2ban settings and rules here
# Example: edit /etc/fail2ban/jail.conf to define rules
# Restart fail2ban: service fail2ban restart
