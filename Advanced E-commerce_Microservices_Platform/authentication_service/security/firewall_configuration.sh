#!/bin/bash

# Script for basic firewall configuration with iptables
# Insert firewall rules here
# Example: iptables -A INPUT -p tcp --dport 80 -j ACCEPT
# Save rules: iptables-save > /etc/iptables/rules.v4
